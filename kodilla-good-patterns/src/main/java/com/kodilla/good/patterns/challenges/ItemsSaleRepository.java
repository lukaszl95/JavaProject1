package com.kodilla.good.patterns.challenges;

import java.time.LocalDate;

public class ItemsSaleRepository implements ItemSaleRepository {

    public boolean createSale(User user, Item item, LocalDate time) {
        return true;
    }
}